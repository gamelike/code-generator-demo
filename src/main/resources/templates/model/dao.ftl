package ${packageName}.dao;

import org.springframework.stereotype.Repository;

import ${packageName}.model.Algorithm;
import com.ums.datasource.util.SpringEntityDAO;

@Repository
public class ${entityName}DAO extends SpringEntityDAO<${entityName}> {
}